{
  "name": "xenolinguist",
  "version": "1.0.0",
  "private": true,
  "main": "electron/dist/main.cjs",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,green \"npm run dev -w client\" \"npm run dev -w server\"",
    "build": "npm run build -w client && npm run build -w server",
    "typecheck": "npm run typecheck -w client && npm run typecheck -w server && tsc -p electron && tsc -p tests",
    "bundle": "node scripts/bundle.mjs",
    "build:desktop": "npm run build -w client && npm run bundle",
    "electron:dev": "concurrently -k -n vite,server,electron -c cyan,green,magenta \"npm run dev -w client\" \"npm run dev -w server\" \"node scripts/wait-and-launch.mjs\"",
    "dist": "npm run build:desktop && electron-builder --config electron/builder.config.cjs",
    "test": "npm test -w server && npm test -w client && npm run test:tooling",
    "test:e2e": "npm run build:desktop && playwright test",
    "test:e2e:run": "playwright test",
    "verify:release": "node scripts/verify-release.mjs",
    "provision:models": "node scripts/provision-models.mjs",
    "stage:runtime": "node scripts/stage-runtime.mjs",
    "test:public": "node scripts/build-public.mjs && playwright test --config playwright.public.config.ts",
    "lint": "npm run lint -w client && node scripts/check-scripts.mjs",
    "test:tooling": "node --test tests/tooling/artifact-layout.test.mjs",
    "check:source": "npm run lint && npm run typecheck && npm test && npm run build:desktop",
    "verify:artifact": "node scripts/verify-artifact-layout.mjs",
    "verify:local-model": "node scripts/verify-local-chat.mjs",
    "audit:record": "node scripts/audit-dependencies.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "concurrently": "^9.1.0",
    "electron": "^42.3.3",
    "electron-builder": "^26.8.1",
    "esbuild": "^0.28.0"
  },
  "dependencies": {
    "electron-updater": "^6.8.3"
  },
  "overrides": {
    "qs": "6.16.0"
  }
}
