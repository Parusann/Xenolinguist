# Implementation progress

The implementation follows the 26-work-package review plan. This log distinguishes implemented boundaries from remaining release and research work.

| Package | Status | Delivered scope |
| --- | --- | --- |
| W01 | Implemented in `c33b581` | Isolated browser and packaged-app probes; reproducible WAV, IPA, concurrency, sandbox and Unicode findings; hashed verification records. |
| W02 | Implemented in `18896b2` | Shared version-2 runtime schemas, guarded import, byte-preserving legacy migration backups, structured errors and manual-confidence provenance. |
| W03 | Implemented with W04 | Revision-checked serialized mutations, persisted retry ledger, atomic replacement, previous snapshots, derived-index repair and one backend owner per data directory. |
| W04 | Implemented with W03 | Durable per-profile save queues, explicit failure/conflict states, field-level rebase, persisted text drafts and phase, and native close/restart recovery. |
| W05 | Implemented in `17ff2c2`; clean Windows verification passed | Explicit runtime resolution, staged production dependencies, pinned asset provisioning, input/error contracts, output provenance and packaged positive/negative inference checks. |
| W06 | Implemented; packaged Windows verification passed | Durable original audio drafts, worker preparation, immutable staged assets, atomic sample/clip saves, explicit analysis, playback/download, delete/undo and recovery checks. See [audio lifecycle](audio-lifecycle.md). |
| W07 | Implemented; packaged Windows verification passed | Versioned persistent practice sessions, exact accepted-form/integer grading, idempotent answer events, separate assistance accounting, generated-key validation and legacy recovery messaging. See [sandbox sessions](sandbox-sessions.md). |
| W08 | Implemented; packaged Windows verification passed | Optional user belief, distinct content counts, explicit exploratory number support/ties, server-owned metric snapshots and truthful guidance labels. See [workspace evidence](workspace-evidence.md). |
| W09 | Implemented; packaged Windows verification passed | Per-launch desktop authentication, Host/Origin and frame restrictions, explicit development pairing, CSP and permissions, bundled fonts and explicit local recording analysis. See [local security](local-security.md). |
| W10 | Implemented; Windows and local-model verification passed | Independent runtime availability, verified local-model selection, explicit setup, bounded cancellable jobs, task budgets and retained profile-scoped proposals. See [runtime jobs](runtime-jobs.md). |
| W11 | Implemented; public and workbench checks passed | Shared versioned Eridian presentation, truthful dictionary widget, pinned release/preview distinction, setup and license corrections, responsive keyboard controls and current documentation. See [presentation claims](public-presentation.md). |
| W12 | Implemented; Windows/Linux source and independent installer CI passed | Locked source gates, tested Pages builds, standalone installed-app acceptance, complete payload hashes, target-specific runtime staging, input-readiness repair and reviewed dependency updates. See [CI and release gates](ci-release-gates.md). |
| W13 | Implemented; Windows/Linux source and independent installer CI passed | Versioned portable archives, bounded staged inspection, checksum/reference validation, preview, collision remapping and atomic restore with complete replacement backups. See [project archives](project-archives.md). |
| W14 | Implemented; verification in progress | Deterministic compiler, executable semantics, frozen datasets, private server grading and version-2 compiler archives. See [deterministic compiler](deterministic-compiler.md). |
| W15 onward | Pending | Baseline evaluation, reproducible research results and subsequent engine work. |

W03 and W04 ship together because requiring revisions without updating the existing client would break saves. Desktop draft persistence uses a stable user-data file store instead of renderer IndexedDB, because the desktop backend's random port changes the browser origin on restart. Browser use retains IndexedDB.

The W03/W04 verification baseline was 108 passing unit tests and three gated native unit checks skipped, plus six repaired browser acceptance checks and four expected failures. The packaged Windows app confirmed pending-save and text-draft recovery after the real close handshake and a new process launch. At that checkpoint, full packaged acceptance failed because IPA returned HTTP 503 from the unresolved external Transformers import; W05 has since repaired it. WAV attachment, sandbox grading/state and Unicode translation remain explicit defects. Full local test artifacts are under ignored `test-results/`; commands and limitations are recorded in [testing.md](testing.md).

No installer release or main-branch merge is implied by these implementation commits. Performance at large profile sizes, broad crash/power-loss certification, automatic corrupt-draft salvage, audio archive garbage collection, and downstream scientific evaluation remain unfinished.

W05 verification supersedes the earlier packaged IPA failure: the current local unpacked executable returns nonempty ARPABET phones with the expected model hash, and the complete release probe passes. Removing the model from the isolated test artifact returns the specific missing-model code. Exact provisioning from the pinned installer and rejection of changed assets were exercised independently. There are 123 passing unit tests and three native unit skips. The [independent Windows workflow](https://github.com/Parusann/Xenolinguist/actions/runs/34648733076) passed for `17ff2c2111a262bebe91bb7aaf0d635b3e844a5b`: a separate verification runner with no source checkout or Transformers installation returned HTTP 200 and 46 ARPABET segments with the expected model hash. The [retained result](verification/w05-clean-windows.json) records the source revision, runtime identity and output. The four browser defects described above remain open. Original quantization provenance is incomplete and recorded explicitly in the asset manifest rather than inferred.

W06 verification repairs the WAV attachment defect. There are 129 passing unit tests with three gated native unit skips. The browser suite now has twelve working checks and three remaining expected failures (sandbox grading/state and Unicode translation); the six affected audio checks passed against the final build. The [packaged Windows record](verification/w06-windows-audio.json) confirms native close/relaunch audio-draft recovery, 48 phone segments from the prepared WAV, byte-identical original retrieval, playback, and the missing-model negative check. The earlier raw fixture inference still returned 46 segments; neither count is an accuracy score. Retained assets use conservative retention; full archive support and reference-aware garbage collection remain open. Desktop onboarding still uses origin-local storage and can reappear when the backend port changes; the acceptance harness suppresses the tour on both launches so it can exercise recovery.

W07 verification repairs substring grading and sandbox navigation loss. There are 153 passing unit tests with three gated native unit skips. The full 18-case browser suite passed: 17 working checks and one remaining expected failure for Unicode translation. All five affected sandbox checks also passed after the final integer sign/token correction. The [Windows recovery record](verification/w07-windows-sandbox.json) confirms the identical session and two events recovered after a failed save and real close/relaunch; a subsequent answer produced a third event and exactly one dictionary entry. Existing audio playback, original checksums, bundled inference and missing-model checks also passed. The model-generation response in sandbox recovery tests is a deterministic fixture. Generated-language semantic validation and blind benchmark support remain future work. W06's independent Windows CI also passed for `ca90c3a8226c71ba0ba6858d2a4248a5b9875a05` ([run](https://github.com/Parusann/Xenolinguist/actions/runs/34674490741)).

W08 verification passes 164 unit tests with three gated native unit skips, type checks, client lint and the desktop build. The full 20-case browser suite passed: 19 working checks and the existing expected Unicode-translation failure. Browser coverage includes duplicate assertions, optional word/rule ratings, clearing a historical rating, backend restart, failed-save history, exact numeral ranges and tied candidates. The [final Windows record](verification/w08-windows-evidence.json) confirms unrated sandbox rewards and visible server-recorded history after real close/relaunch recovery, alongside passing audio playback, bundled inference and the missing-model negative check. Two test fixture races were corrected by awaiting the injected failure response before removing the route override. The legacy writable confidence alias remains for compatibility with pending edits; it represents user belief only. Independent linguistic provenance, calibrated accuracy and installer certification remain outside this package.


W09 verification passes 183 unit tests with three gated native unit skips, type checks, client lint and the desktop build. The full 21-case browser suite has 20 working checks and the existing expected Unicode-translation failure. The actual Vite proxy passes explicit pairing, authenticated mutation, reload and secret-free startup output in an isolated data directory. The [Windows boundary record](verification/w09-windows-boundary.json) records unauthorized HTTP and second-window rejection, blocked foreign navigation, trusted requests across relaunch, synthetic microphone permission and camera rejection, and passing native inference, audio playback and draft/session recovery. The CSP check exposed p5's evaluated JavaScript; replacing its decorative background with Canvas 2D reduced the main JavaScript bundle from 1,598.72 kB to 540.37 kB (before compression). Script policy remains strict. Development cookies have host-level scope; compromised-OS and same-origin renderer attacks remain outside this boundary. No installer certification or independent W09 CI result is claimed.


W10 verification passes 202 unit tests with three gated native unit skips. The full 23-case browser suite has 22 working checks and the existing expected Unicode failure; five affected runtime/sandbox checks also pass against the final client after cancelling pending suggestion timers on unmount. Type checks, lint and desktop build pass. Real HTTP tests prove generation disconnect propagation, current local-model eligibility, download confirmation/cancellation/retry, queue limits and cleanup before slot release. The [Windows runtime record](verification/w10-windows-runtime.json) confirms independent audio/storage availability with chat unavailable, cancellation of a spawned phone process, subsequent native inference and recovery of a saved proposal through close/relaunch. The [local-model record](verification/w10-local-models.json) retains a successful authenticated default-model response, installed cloud/embedding rejection, cancellation after actual token generation and a successful follow-up request. A small synthetic comparison supports retaining the existing default but is not a model-quality benchmark. Disposable phone processes reload the model per job, and partial response checkpoints can lose their newest unflushed fragment on an abrupt crash. Installer certification, broad model evaluation, immutable run provenance and strict cloud-daemon enforcement remain outside this package.


W11 verification passes 204 unit tests with three gated native skips, eight public-site checks and the full 24-case workbench suite (23 working checks plus the expected Unicode failure). Public tests use the actual Pages base path and representative 320–1440 px viewports. The app's new demo is compared against the same seed used by the website; existing profiles remain unchanged. New screenshots show the verified flows, including manual work with chat unavailable. Type checks, lint and both build variants pass. External HEAD/metadata checks confirm the pinned v1.0.0 download and its size, but do not certify that installer. The implementation branch is pushed separately from `main`/Pages; no new installer or live-site deployment is implied. See [W11 evidence](verification/w11-presentation.json). W12 remains next.

W12 completes at implementation revision `0d7563a`. [Windows and Linux source CI](https://github.com/Parusann/Xenolinguist/actions/runs/34899554798) passes locked installation, lint, type checks, 204 unit tests, seven tooling tests, production builds, 25 workbench cases (24 working checks plus the known expected Unicode failure) and eight public checks on each platform. The three gated native unit skips remain explicit. [Independent installer CI](https://github.com/Parusann/Xenolinguist/actions/runs/34899554854) installs the actual 405,328,908-byte NSIS artifact on a separate runner without a source checkout, verifies all 3,772 application files, then passes native IPA/STT/TTS, API boundaries, cancellation, playback, failed-save/draft/session/proposal recovery and missing-model rejection. Local real-model generation, cancellation and subsequent generation also pass. The gates exposed and repaired an undeclared extractor dependency, NSIS destination handling, foreign-target runtime payload mismatch and inconsistent sample-input readiness during recovery. Target selection removes 158,113,768 uncompressed bytes of unused native binaries. Four high native-chain audit package entries remain documented; no public release, signing certification or Pages deployment is implied. [W12 evidence](verification/w12-ci-gates.json) retains hashes, source identities and checkpoints. W13 portable project archives are next.

W13 completes at revision `ff0316d`, following the archive implementation in `0cdc51c`. [Windows and Linux source CI](https://github.com/Parusann/Xenolinguist/actions/runs/35038392588) passes locked installation, lint, type checks, 221 unit tests, seven tooling tests, builds, 27 workbench cases (26 working checks plus the existing expected Unicode failure) and eight public checks per platform. Seventeen archive tests cover an independent clean-directory restore, byte-identical original/analysis audio, relationship remapping, saved history, continued sandbox grading, legacy files, replacement backup recovery and failed publication; malformed and unsupported archives leave live projects untouched. [Independent installed Windows acceptance](https://github.com/Parusann/Xenolinguist/actions/runs/35038392593) verifies all 3,772 files and passes the existing native/recovery gates plus audio-project and sandbox/history archive round trips through the trusted desktop frame. The restored audio hash matches the original fixture. Bundled ZIP dependency licenses ship with the installer. The source retains three gated native unit skips and four high native-chain audit package entries. Archives cap uploads/expanded data at 256 MiB, exclude unsaved drafts/models, and conservatively retain orphan assets and backups. [W13 evidence](verification/w13-project-archives.json) records exact revisions, reports, payload hashes and the CI artifact ZIP digest. No public installer or Pages deployment is implied. W14's deterministic language compiler is next.
